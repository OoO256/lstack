# PM Agent Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PM agent가 사용자 인터뷰 → 코드 스코핑 → 계획 수립 → agent pool에서 실행자 선택/dispatch → 병렬 검증 → 문서화까지 오케스트레이션. 단일 `tasks.json`으로 모든 상태 관리.

**Architecture:** PM은 skill(`skills/pm/SKILL.md`)로 진입하며, tasks.json을 중심으로 phase를 진행한다. 구현은 외부 플러그인 agent pool에서 선택, 검증은 AC 항목별 외부 agent 병렬 dispatch. check-jsonschema 기반 validation 훅으로 tasks.json 스키마 보호.

**Tech Stack:** Claude Code plugin (YAML frontmatter + markdown), tasks.json (단일 SOT), check-jsonschema (schema validation), gh CLI.

**설계 원칙:** `docs/spec/PRINCIPLE.md` 참조 — context 분리, 자기 평가 금지, 검증 기본, 최소 범위 병렬 검증, 단일 SOT.

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `skills/pm/SKILL.md` | PM 오케스트레이션 스킬 (7 phase) |
| Create | `skills/pm/tasks-schema.json` | tasks.json JSON 구조 명세 |
| Create | `hooks/scripts/validate-tasks.sh` | tasks.json 수정 시 스키마 validation |
| Modify | `hooks/hooks.json` | tasks validation 훅 등록 |
| Modify | `CLAUDE.md` | PM skill, 훅 문서화 |
| Modify | `docs/spec/ARCHITECTURE.md` | PM 흐름 + tasks.json 구조 추가 |

---

### Task 1: Tasks Schema

**Files:**
- Create: `skills/pm/tasks-schema.json`

- [ ] **Step 1: Write the schema definition**

이 파일은 tasks.json의 구조를 정의한다. agent들이 참조하는 스키마 명세이자, validation 스크립트가 체크하는 기준.

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["goal", "requirements", "tasks"],
  "properties": {
    "goal": {
      "type": "string",
      "description": "사용자 의도 한 줄 요약"
    },
    "requirements": {
      "type": "array",
      "description": "사용자 요구사항 + 설계 결정사항. 모든 task는 이를 충족해야 한다.",
      "items": {
        "type": "object",
        "required": ["id", "type", "content", "status", "acceptance_criteria"],
        "properties": {
          "id": { "type": "string", "pattern": "^R[0-9]+$" },
          "type": {
            "type": "string",
            "enum": ["requirement", "design_decision"],
            "description": "requirement: 사용자 요구사항, design_decision: Phase 2에서 결정한 설계 사항"
          },
          "content": { "type": "string", "description": "요구사항 또는 설계 결정 내용" },
          "rationale": { "type": "string", "description": "design_decision인 경우 결정 근거" },
          "status": { "type": "string", "enum": ["pending", "pass", "fail"] },
          "acceptance_criteria": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["id", "check", "verify_agent", "status"],
              "properties": {
                "id": { "type": "string", "pattern": "^AC[0-9]+$" },
                "check": { "type": "string", "description": "구체적 검증 항목" },
                "verify_agent": { "type": "string", "description": "검증할 외부 agent" },
                "status": { "type": "string", "enum": ["pending", "pass", "fail"] }
              }
            }
          }
        }
      }
    },
    "tasks": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "action", "status", "acceptance_criteria"],
        "properties": {
          "id": { "type": "string", "pattern": "^T[0-9]+$" },
          "action": { "type": "string" },
          "status": {
            "type": "string",
            "enum": ["pending", "in_progress", "done", "verified", "failed"]
          },
          "agent": {
            "type": "string",
            "description": "실행할 agent (예: general-purpose, superpowers:code-reviewer, oh-my-claudecode:executor)"
          },
          "depends_on": {
            "type": "array",
            "items": { "type": "string", "pattern": "^T[0-9]+$" }
          },
          "acceptance_criteria": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["id", "check", "verify_agent", "status"],
              "properties": {
                "id": { "type": "string", "pattern": "^AC[0-9]+$" },
                "check": {
                  "type": "string",
                  "description": "구체적 검증 항목 — context 없는 다른 agent가 봐도 동일하게 판단 가능해야 함"
                },
                "verify_agent": {
                  "type": "string",
                  "description": "검증할 외부 agent (예: superpowers:code-reviewer, hoyeon:qa-verifier). 필수."
                },
                "status": {
                  "type": "string",
                  "enum": ["pending", "pass", "fail"]
                }
              }
            }
          },
          "worklog": {
            "type": "object",
            "description": "task 완료 후 agent가 기록하는 작업 로그",
            "properties": {
              "summary": { "type": "string", "description": "작업 요약" },
              "decisions": {
                "type": "array",
                "items": { "type": "string" },
                "description": "구현 중 내린 의사결정"
              },
              "insights": {
                "type": "array",
                "items": { "type": "string" },
                "description": "구현 중 발생한 암묵지 — 코드에 드러나지 않는 발견, 주의점, 노하우"
              },
              "changes": {
                "type": "array",
                "items": { "type": "string" },
                "description": "동작 변경사항 — 기존 동작이 어떻게 바뀌었는지"
              }
            }
          }
        }
      }
    }
  }
}
```

Write to `skills/pm/tasks-schema.json`.

---

### Task 2: Tasks Validation Hook

**Files:**
- Create: `hooks/scripts/validate-tasks.sh`
- Modify: `hooks/hooks.json`

- [ ] **Step 1: Write validation script**

```bash
#!/bin/bash
# PostToolUse hook: validate tasks.json after Write/Edit
# Uses check-jsonschema for JSON Schema validation

input=$(cat)
tool_name=$(echo "$input" | jq -r '.tool_name // empty')
tool_input_path=$(echo "$input" | jq -r '.tool_input.file_path // empty')

if [ "$tool_name" = "Write" ] || [ "$tool_name" = "Edit" ]; then
  if echo "$tool_input_path" | grep -q "tasks.json"; then
    if [ -f "$tool_input_path" ]; then
      check-jsonschema --schemafile "${CLAUDE_PLUGIN_ROOT}/skills/pm/tasks-schema.json" "$tool_input_path" 2>&1
    fi
  fi
fi
```

Write to `hooks/scripts/validate-tasks.sh`, then `chmod +x`.

- [ ] **Step 2: Update hooks.json (Write|Edit matcher 추가)**

Add a second PostToolUse matcher for Write and Edit:

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/commit-document-reminder.sh\"",
            "async": true
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/validate-tasks.sh\"",
            "async": false
          }
        ]
      }
    ]
  }
}
```

Write to `hooks/hooks.json`.


---

### Task 3: PM Skill

**Files:**
- Create: `skills/pm/SKILL.md`

- [ ] **Step 1: Write PM skill definition**

```markdown
---
name: pm
description: |
  This skill should be used when the user says "/pm", "pm", "프로젝트 시작",
  "이거 만들어", "이거 고쳐", or gives a task that requires planning, scoping,
  and multi-step execution. Orchestrates the full workflow: interview → scope →
  plan → execute → verify → document.
---

# PM — Project Manager Orchestrator

사용자의 요청을 인터뷰로 명확히 하고, 코드를 스코핑하고, tasks.json으로 계획을 세우고, agent pool에서 적절한 실행자를 선택하여 dispatch하고, 검증하고, 문서화한다.

**설계 원칙:** `docs/spec/PRINCIPLE.md` 참조.

## Phase 1: Interview

사용자의 의도를 파악하고 명확하게 한다.

1. 사용자의 요청을 읽고, 부족한 정보를 식별한다.
2. 한 번에 하나씩 질문한다 (가능하면 multiple choice).
3. 다음이 명확해질 때까지 계속한다:
   - 무엇을 원하는지 (goal)
   - 왜 원하는지 (motivation)
   - 성공 기준이 무엇인지 (acceptance criteria)
   - 범위 밖은 무엇인지 (non-goals)

결과: `goal`과 `requirements` 초안 정리.

## Phase 2: Design

코드 상태를 확인하고, 구현을 시뮬레이션하고, 클린한 구조와 검증 방법을 설계하고, tasks.json을 작성한다.
순차적으로 3개 agent를 dispatch한다. 각 agent는 이전 agent의 결과를 입력으로 받는다.

### 2.1~2.3: architect agent

**2.1 수정 범위 파악**
1. 관련 파일/모듈을 Glob, Grep, Read로 탐색.
2. 수정이 필요한 파일 목록을 작성.
3. 의존성과 영향 범위를 확인.

**2.2 구현 시뮬레이션**
1. 요구사항을 코드 변경으로 머릿속에서 변환해본다.
2. 변경이 기존 코드에 미치는 영향을 추적한다.
3. 예상되는 난관과 엣지 케이스를 식별한다.

**2.3 디자인 패턴 결정**
1. 기존 코드베이스의 패턴을 파악한다.
2. 요구사항을 최대한 클린하게 구현할 수 있는 구조를 설계한다.
   - 책임 분리, 인터페이스 경계, 의존성 방향
   - 기존 패턴을 따르되, 개선이 필요하면 제안
3. 설계 결정과 근거를 기록한다.

결과: 수정 범위 + 설계 결정 → tasks.json에 `design_decision` requirements로 기록.

### 2.4: tester agent

입력: architect agent의 결과 (수정 범위, 설계 결정)

1. 요구사항을 검증하기 위한 최소한의 테스트 케이스를 도출한다.
   - 핵심 동작을 커버하되, 중복 테스트를 배제
   - happy path + 핵심 edge case만 — 불필요하게 많은 케이스 금지
2. 각 테스트가 어떤 requirement/AC를 검증하는지 매핑한다.
3. 테스트 실행 방법 (command, assertion, inspection)을 결정한다.

결과: 테스트 시나리오 → tasks.json의 acceptance_criteria에 반영.

### 2.5: planner agent

입력: architect + tester 결과

1. 작업을 task로 분해한다.
2. 각 task마다 구체적인 acceptance criteria를 정의한다:
   - **원칙 2.1**: context 없는 다른 agent가 봐도 동일하게 판단 가능한 수준
   - **원칙 2.1**: 불필요하게 엄밀하지 않게
3. task 간 의존성을 설정한다.
4. 각 task에 적절한 agent를 지정한다 (agent pool에서 선택).
5. tasks.json을 작성한다.

**사용자에게 tasks.json을 보여주고 승인을 받은 후 Phase 3로 진행.**

### Execute Agent Pool

task 유형에 따라 agent를 선택:

| 유형 | Agent | 비고 |
|------|-------|------|
| 구현 | `oh-my-claudecode:executor` | 코드 변경, multi-file 처리. sonnet |
| 테스트 작성 | `oh-my-claudecode:test-engineer` | TDD + unit/integration/e2e + 커버리지 갭 분석 |
| 디버깅 | `oh-my-claudecode:debugger` | 근본 원인 분석 + 최소 수정 제안 |
| 디버깅 (체계적) | `superpowers:systematic-debugging` | 4-phase 근본 원인 추적 |
| 리팩토링 | `oh-my-claudecode:code-simplifier` | 동작 유지하면서 가독성/유지보수성 개선. opus |
| 탐색 | `oh-my-claudecode:explore` | 코드베이스 검색, 패턴 찾기. haiku |
| fallback | `general-purpose` | 위 agent가 모두 안 맞을 때만 |

### Verify Agent Pool

AC 검증에 사용할 agent:

| 용도 | Agent | 비고 |
|------|-------|------|
| 테스트 검증 | `oh-my-claudecode:test-engineer` | 테스트 전략 + 작성 + 커버리지 분석 |
| 코드 품질 | `superpowers:code-reviewer` | diff 기반 리뷰, Critical/Important/Minor 분류 |
| 완료 검증 | `oh-my-claudecode:verifier` | AC 기반 증거 수집, pass/fail 판정 |
| 비판적 리뷰 | `oh-my-claudecode:critic` | 다관점 결함/갭 탐지. opus |
| 보안 감사 | `oh-my-claudecode:security-reviewer` | OWASP Top 10, secrets, auth/authz. opus |
| 보안 감사 (심층) | `gstack:cso` | STRIDE, supply chain, CI/CD, LLM 보안 |

### Design Agent Pool

Phase 2에서 architect/tester/planner 외에 추가로 활용 가능:

| 용도 | Agent | 비고 |
|------|-------|------|
| 아키텍처 리뷰 | `oh-my-claudecode:architect` | 코드 기반 구조 분석, file:line 증거. read-only, opus |
| 요구사항 분석 | `oh-my-claudecode:analyst` | AC 도출, 갭/엣지케이스 발견. opus |
| 갭 분석 | `hoyeon:gap-analyzer` | 누락 요구사항, AI 함정, 오버엔지니어링 탐지 |
| 트레이드오프 | `hoyeon:tradeoff-analyzer` | 변경별 리스크 (LOW/MED/HIGH), 더 간단한 대안 |
| 외부 조사 | `hoyeon:external-researcher` | 라이브러리, API, 마이그레이션 웹 조사 |
| UX 리뷰 | `hoyeon:ux-reviewer` | UX 흐름 영향 분석, 더 간단한 대안 |
| 엔지니어링 리뷰 | `gstack:plan-eng-review` | 아키텍처, 엣지케이스, 테스트 커버리지 인터랙티브 리뷰 |

### tasks.json 작성

스키마는 `skills/pm/tasks-schema.json` 참조. 예시:

```json
{
  "goal": "인증 API 구현",
  "requirements": [
    {
      "id": "R1", "type": "requirement", "content": "JWT 기반 로그인 API가 동작한다", "status": "pending",
      "acceptance_criteria": [
        { "id": "AC1", "check": "POST /auth/login이 유효한 credential에 200 + JWT 반환", "verify_agent": "oh-my-claudecode:test-engineer", "status": "pending" },
        { "id": "AC2", "check": "잘못된 credential에 401 반환", "verify_agent": "oh-my-claudecode:test-engineer", "status": "pending" }
      ]
    },
    {
      "id": "R2", "type": "requirement", "content": "기존 코드 패턴을 따른다", "status": "pending",
      "acceptance_criteria": [
        { "id": "AC3", "check": "auth 모듈이 기존 미들웨어 패턴을 따른다", "verify_agent": "superpowers:code-reviewer", "status": "pending" }
      ]
    },
    {
      "id": "R3", "type": "design_decision", "content": "passport.js 대신 직접 미들웨어로 구현", "rationale": "의존성 최소화 + 기존 미들웨어 패턴과 일관성", "status": "pending",
      "acceptance_criteria": [
        { "id": "AC4", "check": "passport.js 의존성이 없고 직접 미들웨어로 구현되어 있다", "verify_agent": "superpowers:code-reviewer", "status": "pending" }
      ]
    }
  ],
  "tasks": [
    {
      "id": "T1",
      "action": "로그인 엔드포인트 구현",
      "status": "pending",
      "agent": "general-purpose",
      "depends_on": [],
      "acceptance_criteria": [
        { "id": "AC1", "check": "POST /auth/login이 유효한 credential에 200 + JWT 반환", "verify_agent": "oh-my-claudecode:test-engineer", "status": "pending" },
        { "id": "AC2", "check": "잘못된 credential에 401 반환", "verify_agent": "oh-my-claudecode:test-engineer", "status": "pending" }
      ],
      "worklog": {
        "summary": "bcrypt + jsonwebtoken으로 로그인 엔드포인트 구현",
        "decisions": ["passport.js 대신 직접 미들웨어 구현 — 의존성 최소화"],
        "insights": ["기존 error handler가 401을 잡아서 generic message로 바꾸고 있었음 — auth 에러는 별도 처리 필요"],
        "changes": ["POST /auth/login 엔드포인트 추가", "401 응답이 기존 generic error 대신 auth 전용 메시지 반환"]
      }
    }
  ]
}
```

tasks.json 경로: 프로젝트 루트 또는 `.lstack/tasks.json`.

**사용자에게 tasks.json을 보여주고 승인을 받은 후 Phase 4로 진행.**

## Phase 3: Execute

task를 순서대로 (또는 의존성 없으면 병렬로) 실행한다.

각 task마다:

1. **tasks.json에서 task 읽기** — status를 `in_progress`로 업데이트.

2. **Agent dispatch** — fresh subagent로 spawn:
   ```
   Agent({
     subagent_type: "<task.agent>",
     prompt: <포함>
       - task.action (무엇을 할지)
       - task.acceptance_criteria (검증 기준 — 이것을 통과해야 함)
       - 관련 requirements (이 task가 충족해야 할 요구사항)
   })
   ```

3. **완료 시** — tasks.json에서 task status를 `done`으로 업데이트.

4. **검증 (Phase 5로)** — 바로 검증 진행.

## Phase 4: Verify

**원칙 1.2: 구현한 agent가 아닌 별도 verifier가 검증.**
**원칙 2.2: AC 항목별 최소 범위 병렬 검증.**

각 task의 acceptance_criteria에 대해:

1. **AC 항목별 외부 검증 agent를 병렬 dispatch:**
   ```
   // AC가 3개면 3개의 agent를 동시에 spawn (각 AC의 verify_agent 사용)
   Agent({
     subagent_type: "<AC.verify_agent>",
     prompt: <포함>
       - AC.check (검증할 항목)
       - task.action (무엇이 구현되었는지)
       - 관련 파일 경로
       - "pass 또는 fail로만 답하고, fail이면 구체적 이유를 포함"
   })
   ```

2. **결과 수집:**
   - 모든 AC가 pass → tasks.json에서 task status를 `verified`로 업데이트.
   - 하나라도 fail → task status를 `failed`로 업데이트.

3. **Ralph-loop (fail 시):**
   - 실패한 AC의 evidence와 suggestion을 포함하여 agent를 재dispatch.
   - 재구현 후 다시 Phase 5 검증.
   - 최대 3회 반복. 3회 실패 시 사용자에게 에스컬레이션.

4. **모든 task가 verified 되면** → 전체 검증:
   - tasks.json의 모든 task.status가 `verified`인지 확인.
   - goal에 대한 최종 검증 agent dispatch (전체 맥락에서 goal 달성 여부).

## Phase 5: Document

`/document` 스킬을 호출하여 worklog + spec 업데이트.

추가로 tasks.json 자체도 docs/plan/ 에 아카이빙:
```bash
cp tasks.json docs/plan/YYYY-MM-DD-<goal-slug>-tasks.json
```

## Phase 6: Compound

작업 과정에서 하니스 자체의 문제점이 발견되었는지 검토한다.

1. **하니스 문제 감지:**
   - PM 워크플로우 중 비효율적이었던 부분이 있었는가
   - agent 선택이 부적절했거나 누락된 agent 유형이 있었는가
   - tasks.json 스키마에 부족한 필드가 있었는가
   - 검증 과정에서 반복적으로 실패한 패턴이 있었는가

2. **문제가 발견되면:**
   - `/compound` 스킬을 호출하여 harness-sage가 개선 PR 생성
   - 사용자에게 발견된 문제와 개선 방향을 보고

3. **문제가 없으면:**
   - 스킵
```

Write to `skills/pm/SKILL.md`.

---

### Task 4: Update CLAUDE.md + ARCHITECTURE.md

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/spec/ARCHITECTURE.md`

- [ ] **Step 1: CLAUDE.md에 PM skill, validate-tasks hook 추가**

- [ ] **Step 2: ARCHITECTURE.md에 PM orchestration flow 추가**

Add a new section after Compound Self-Improvement Loop:

```markdown
## PM Orchestration

사용자 요청을 plan → execute → verify 하는 핵심 워크플로우.

### Flow

```
사용자 요청
    │
    ▼
PM Skill (메인 컨텍스트)
    │  Phase 1: Interview — 의도 파악
    │  Phase 2: Design — architect → tester → planner → tasks.json + 사용자 승인
    │  Phase 3: Execute — task별 agent dispatch (pool에서 선택)
    │  Phase 4: Verify — AC별 외부 agent 병렬 dispatch + ralph-loop
    │  Phase 5: Document — /document 호출 + tasks.json 아카이빙
    │  Phase 6: Compound — 하니스 문제 발견 시 /compound로 개선 PR
    │
    ▼
tasks.json (단일 SOT)
    │
    ├─ Agent Pool (구현) ──── general-purpose, superpowers:code-reviewer, ...
    └─ Verifier (검증) ────── AC 항목별 병렬, read-only
```

### tasks.json

단일 SOT. 스키마: `skills/pm/tasks-schema.json`.

| Field | Description |
|-------|-------------|
| goal | 사용자 의도 한 줄 요약 |
| requirements[] | id, type (requirement\|design_decision), content, rationale?, status, acceptance_criteria[] |
| requirements[].acceptance_criteria[] | id, check, verify_agent, status (pending/pass/fail) |
| tasks[] | id, action, status, agent, depends_on, acceptance_criteria[], worklog |
| tasks[].acceptance_criteria[] | id, check, verify_agent, status (pending/pass/fail) |
| tasks[].worklog | summary, decisions, insights, changes |

### Components

| Component | Path | Role |
|-----------|------|------|
| pm skill | `skills/pm/SKILL.md` | 7-phase 오케스트레이션 |
| tasks schema | `skills/pm/tasks-schema.json` | 스키마 정의 |
| validation hook | `hooks/scripts/validate-tasks.sh` | Write/Edit 시 스키마 체크 |
```

---

### Task 5: End-to-End Verification

- [ ] **Step 1: Verify complete file structure**

Run: `find . -path ./references -prune -o -path ./.git -prune -o -print | sort`
Expected: All new files present:
```
./agents/harness-sage.md
./hooks/scripts/commit-document-reminder.sh
./hooks/scripts/validate-tasks.sh
./hooks/hooks.json
./skills/compound/SKILL.md
./skills/compound/references.md
./skills/document/SKILL.md
./skills/pm/SKILL.md
./skills/pm/tasks-schema.json
```

- [ ] **Step 2: Verify all agent frontmatter**

Run:
```bash
for f in agents/*.md; do echo "=== $f ===" && head -3 "$f"; done
```
Expected: Each file has `---` and `name:`.

- [ ] **Step 3: Verify all skill frontmatter**

Run:
```bash
for f in skills/*/SKILL.md; do echo "=== $f ===" && head -3 "$f"; done
```
Expected: Each file has `---` and `name:`.

- [ ] **Step 4: Verify tasks schema is valid JSON**

Run: `jq '.' skills/pm/tasks-schema.json > /dev/null && echo "valid"`
Expected: "valid"

- [ ] **Step 5: Run check-jsonschema with valid input**

Run:
```bash
cat > /tmp/test-e2e.json << 'EOF'
{"goal":"test","requirements":[{"id":"R1","content":"x","status":"pending","acceptance_criteria":[{"id":"AC1","check":"y","verify_agent":"superpowers:code-reviewer","status":"pending"}]}],"tasks":[{"id":"T1","action":"do","status":"pending","acceptance_criteria":[{"id":"AC1","check":"y","verify_agent":"superpowers:code-reviewer","status":"pending"}]}]}
EOF
check-jsonschema --schemafile skills/pm/tasks-schema.json /tmp/test-e2e.json
```
Expected: Validation passed.

- [ ] **Step 6: Run check-jsonschema with invalid input**

Run: `echo '{"tasks":[]}' | check-jsonschema --schemafile skills/pm/tasks-schema.json -`
Expected: Validation error (missing goal, requirements).

- [ ] **Step 7: Push**

Run: `git push origin main`
Expected: All commits pushed.
